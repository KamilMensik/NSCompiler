#include "include/nyassembly_builder.h"

void build_nyassembly(ast_T *ast, FILE *output) {
    switch (ast->type) {
        case AST_COMPOUND:
            break;
        case AST_ATOM:
            
            break;
    }
}